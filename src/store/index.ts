import { createStore } from "vuex";
import photoDetailsStore from '@/store/photo-details'
import type { AppStore, RootState } from "@/models/store/AppStore";

export default createStore({
  modules: {
  }
});